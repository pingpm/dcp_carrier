# 承运商端 uniapp

承运商端用于承运商认证入驻、维护线路、确认订单、履约运输、钱包充值和查看流水。

## 启动

当前目录为 uniapp 基础工程，可通过 HBuilderX 打开并运行到微信开发者工具。

后续补齐 CLI 依赖后，可从仓库根目录运行：

```bash
npm run dev:carrier
```

## 约定

- 承运商端默认建议使用蓝色运营工具风格。
- 未认证承运商可以维护线路，但不能被搜索、不能接单。
- 内部状态值必须使用 `docs/schema/enums.md` 中的英文枚举。
